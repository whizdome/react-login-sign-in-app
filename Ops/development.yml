apiVersion: apps/v1
kind: Deployment
metadata:
  name: whizapp-deployment
spec:
  selector:
    matchLabels:
      app: myapp
  replicas: 2
  template:
    metadata:
      labels:
        app: whizapp
    spec:
      containers:
        - name: whizapp
          image: whizapp/whizapp:latest
          ports:
            - containerPort: 8080